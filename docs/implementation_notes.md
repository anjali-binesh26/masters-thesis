### Progress
Week of: 20.07
- Reviewed the NetSight architecture and execution flow.
- Identified the responsibilities of the agent components.
- Successfully built the Docker image.
- Verified the project configuration.

Week of: 27.07
- Verified e2e NetSight execution.
- Began use case 1 by analysing Amarisoft QoS configuration.
- Iteratively refined the system prompt to improve the quality and structure of the generated output.

| Run | Observation |
|------|-------------|
| 1 | Completed successfully (239.2 s). Correctly identified QoS parameters but produced very verbose documentation-style output focused primarily on configuration files. |
| 2 | Timed out after prompt modifications. Prompt was too broad and encouraged exhaustive analysis of the documentation. |
| 3 | Completed successfully (193.7 s). Produced concise tabular interface definitions with parameter locations and identified both configuration file and Remote API interfaces. Some API mappings may require manual verification. |

Week of: 03.08

#### Use Case 0 - Remote API Access

- Implemented a reusable `AmarisoftAPI` Python wrapper for the Amarisoft Remote API.
- Successfully established a WebSocket connection from the workstation to the Amarisoft Callbox.
- Retrieved runtime information using the documented `ue_get` API message.
- Successfully received and parsed JSON responses containing UE registration state and active PDU session information.
- This provides the execution layer that will later receive requests generated by the LLM/controller.


### Current Issue

- LLM connectivity issue resolved by enabling DHCP on the workstation, allowing hostname resolution of the lab-hosted LLM.
- Current LLM outputs still require refinement. The generated Remote API requests and interface definitions are useful but may include inferred fields or mappings that require verification against the Amarisoft documentation.

### Next Steps

- Refine the LLM prompt to generate executable Remote API requests rather than API schema descriptions.
- Validate generated Remote API requests against the Amarisoft documentation.
- Extend the Remote API wrapper to support additional documented management operations (e.g., `stats`, session information).
- Develop a controller that consumes LLM output, validates operator intent, and generates execution requests.
- Integrate the controller with the Remote API wrapper for read-only management tasks (Use Case 0).
- Resume development of Use Case 1 by extending the controller to support simple single-parameter configuration changes.